import {SafeAreaView,StyleSheet,Text} from 'react-native';import {useAlert} from '../../src/modules/alert/AlertProvider';export default function Active(){const {alert}=useAlert();return <SafeAreaView style={s.page}><Text style={s.title}>{alert.active?'Alerta SOS activa':'No hay alerta activa'}</Text><Text style={s.copy}>La ubicación periódica, llamadas, chat, evidencias y finalización se agregan en HUs posteriores.</Text></SafeAreaView>}const s=StyleSheet.create({page:{flex:1,padding:24,gap:16,backgroundColor:'#fff1f2'},title:{fontSize:28,fontWeight:'800',color:'#881337'},copy:{color:'#475569'}});
