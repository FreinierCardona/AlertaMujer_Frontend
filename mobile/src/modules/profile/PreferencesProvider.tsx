import AsyncStorage from '@react-native-async-storage/async-storage';
import {createContext,useContext,useEffect,useState,type ReactNode} from 'react';
export type Language='es'|'en';export type Theme='light'|'dark';type V={language:Language;theme:Theme;ready:boolean;setLanguage:(v:Language)=>void;setTheme:(v:Theme)=>void};const C=createContext<V|null>(null);
export function usePreferences(){const v=useContext(C);if(!v)throw new Error('Preferences provider missing');return v;}
export function PreferencesProvider({children}:{children:ReactNode}){const [language,setLanguageState]=useState<Language>('es');const [theme,setThemeState]=useState<Theme>('light');const [ready,setReady]=useState(false);useEffect(()=>{Promise.all([AsyncStorage.getItem('@alertamujer/language'),AsyncStorage.getItem('@alertamujer/theme')]).then(([l,t])=>{if(l==='en'||l==='es')setLanguageState(l);if(t==='dark'||t==='light')setThemeState(t);}).finally(()=>setReady(true));},[]);const setLanguage=(v:Language)=>{setLanguageState(v);void AsyncStorage.setItem('@alertamujer/language',v)};const setTheme=(v:Theme)=>{setThemeState(v);void AsyncStorage.setItem('@alertamujer/theme',v)};return <C.Provider value={{language,theme,ready,setLanguage,setTheme}}>{children}</C.Provider>}
